<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DefectVision.UI.ViewModels"
             xmlns:i="https://github.com/projektanker/icons.avalonia"
             x:Class="DefectVision.UI.Views.AnnotationView"
             x:DataType="vm:AnnotationViewModel">

    <Grid ColumnDefinitions="220,*,280">

        <!-- ===== Left ===== -->
        <Grid Grid.Column="0" RowDefinitions="Auto,*,Auto,Auto">

            <Border Grid.Row="0" Background="{StaticResource PanelLightBlueBrush}" Classes="panel-header">
                <Grid ColumnDefinitions="*,Auto">
                    <TextBlock x:Name="LblImageList" VerticalAlignment="Center" Classes="h1" />
                    <Button Grid.Column="1" x:Name="BtnImport" FontSize="11"
                            Command="{Binding ImportImagesCommand}" Padding="8,2" />
                </Grid>
            </Border>

            <ListBox Grid.Row="1" ItemsSource="{Binding ImageList}"
                     SelectedItem="{Binding SelectedImage}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="vm:ImageListItem">
                        <Grid ColumnDefinitions="Auto,*,Auto" Margin="2">
                            <Ellipse Grid.Column="0" Width="8" Height="8" Margin="0,0,6,0"
                                     VerticalAlignment="Center"
                                     Fill="{Binding StatusColor}" />
                            <TextBlock Grid.Column="1" Text="{Binding FileName}"
                                       FontSize="11" TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{Binding AnnotationCount}"
                                       VerticalAlignment="Center" Margin="4,0,0,0" Classes="secondary" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="2" Classes="panel-footer">
                <StackPanel Spacing="4">
                    <TextBlock FontSize="11">
                        <Run x:Name="RunLabeledPrefix" />
                        <Run Text="{Binding LabeledCount}" FontWeight="Bold" />
                        <Run Text=" / " />
                        <Run Text="{Binding TotalCount}" />
                    </TextBlock>
                    <ProgressBar Value="{Binding LabelProgress}" Maximum="100" Height="4" />
                </StackPanel>
            </Border>

            <!-- ★ Class list: use ListBox so user can click to select ★ -->
            <Border Grid.Row="3" Padding="8" Background="{StaticResource PanelExtraLightGrayBrush}" BorderBrush="{StaticResource BorderLightGrayBrush}" BorderThickness="0,1,0,0">
                <StackPanel Spacing="4">
                    <TextBlock x:Name="LblDefectClasses" Text="Defect Classes" FontWeight="Bold" FontSize="12" />

                    <ListBox ItemsSource="{Binding ClassList}"
                             SelectedItem="{Binding SelectedClassItem}"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="0"
                             MinHeight="30">
                        <ListBox.ItemTemplate>
                            <DataTemplate x:DataType="vm:ClassDisplayItem">
                                <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,1" Height="24">
                                    <Border Grid.Column="0" Width="14" Height="14"
                                            CornerRadius="2" Margin="0,0,6,0"
                                            Background="{Binding Color}" />
                                    <TextBlock Grid.Column="1" Text="{Binding Name}"
                                               FontSize="12" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2" Text="{Binding Shortcut}"
                                               FontSize="10" Foreground="{StaticResource TextTertiaryBrush}"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Button x:Name="BtnAddClass" FontSize="11" HorizontalAlignment="Stretch"
                            Command="{Binding AddClassCommand}" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- ===== Center ===== -->
        <Grid Grid.Column="1" Background="{StaticResource CanvasBackgroundBrush}">

            <Border VerticalAlignment="Top" ZIndex="10" Background="{StaticResource ToolbarBackgroundBrush}"
                    Padding="4,2" CornerRadius="0,0,4,4">
                <StackPanel Orientation="Horizontal" Spacing="2">
                    <RadioButton x:Name="RbRect" GroupName="Tool"
                                 IsChecked="{Binding IsRectTool}"
                                 Classes="tool-button" Foreground="{StaticResource TextLightBrush}">
                        <StackPanel>
                            <i:Icon Classes="icon" Value="mdi-square-outline" />
                            <TextBlock Text="矩形框" />   
                        </StackPanel>
                    </RadioButton>

                    <RadioButton x:Name="RbPolygon" GroupName="Tool"
                                 IsChecked="{Binding IsPolygonTool}"
                                 Classes="tool-button" Foreground="{StaticResource TextLightBrush}">
                        <StackPanel>
                            <i:Icon Classes="icon" Value="mdi-circle-edit-outline" />
                            <TextBlock Text="描边"/>   
                        </StackPanel>
                    </RadioButton>

                    <RadioButton x:Name="RbPolyline" GroupName="Tool"
                                 IsChecked="{Binding IsPolylineTool}"
                                 Classes="tool-button" Foreground="{StaticResource TextLightBrush}">
                        <StackPanel>
                            <i:Icon Classes="icon" Value="mdi-vector-polygon" />
                            <TextBlock Text="折线"/>   
                        </StackPanel>
                    </RadioButton>

                    <Border Width="1" Background="{StaticResource SeparatorBrush}" Margin="8,4" />

                    <Button x:Name="BtnUndo" Command="{Binding UndoCommand}"
                            Classes="tool-button" Background="Transparent" Foreground="{StaticResource TextLightBrush}">
                        <StackPanel>
                            <i:Icon Classes="icon" Value="mdi-undo" />
                            <TextBlock Text="撤销"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnRedo" Command="{Binding RedoCommand}"
                            Classes="tool-button" Background="Transparent" Foreground="{StaticResource TextLightBrush}">
                        <StackPanel>
                            <i:Icon Classes="icon" Value="mdi-redo" />
                            <TextBlock Text="重做" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnDeleteSel" Command="{Binding DeleteSelectedCommand}"
                            Classes="tool-button" Background="Transparent" Foreground="{StaticResource DeleteButtonForegroundBrush}">
                        <StackPanel>
                            <i:Icon Classes="icon" Value="mdi-delete-outline"></i:Icon>
                            <TextBlock Text="删除" />
                        </StackPanel>
                    </Button>

                    <Border Width="1" Background="{StaticResource SeparatorBrush}" Margin="8,4" />

                    <StackPanel VerticalAlignment="Center" Margin="8,0" Spacing="2">
                         <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="10" Text="当前类别" />
                         <TextBlock Text="{Binding SelectedClassName}" Foreground="{StaticResource HighlightBrush}" 
                                    FontWeight="Bold" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <Border Width="1" Background="{StaticResource SeparatorBrush}" Margin="8,4" />

                    <TextBlock x:Name="TxtToolHint" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="8,0" />
                </StackPanel>
            </Border>

            <Border x:Name="CanvasContainer" Margin="0,36,0,24" ClipToBounds="True"
                    Background="Transparent"
                    PointerPressed="Canvas_PointerPressed"
                    PointerMoved="Canvas_PointerMoved"
                    PointerReleased="Canvas_PointerReleased"
                    PointerWheelChanged="Canvas_PointerWheelChanged">
                <Canvas x:Name="AnnotationCanvas">
                    <Image x:Name="CanvasImage" Source="{Binding CurrentImageSource}"
                           Stretch="None" />
                </Canvas>
            </Border>

            <Border VerticalAlignment="Bottom" ZIndex="10" Background="{StaticResource InfoBarBackgroundBrush}" Padding="8,4">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <TextBlock Classes="muted">
                        <Run x:Name="RunFileLabel" />
                        <Run Text="{Binding CurrentFileName}" />
                    </TextBlock>
                    <TextBlock Classes="muted">
                        <Run x:Name="RunSizeLabel" />
                        <Run Text="{Binding ImageSizeText}" />
                    </TextBlock>
                    <TextBlock Classes="muted">
                        <Run x:Name="RunZoomLabel" />
                        <Run Text="{Binding ZoomPercentText}" />
                    </TextBlock>
                    <TextBlock x:Name="TxtToolMode" Foreground="{StaticResource HighlightBrush}" FontSize="11" Margin="16,0,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- ===== Right ===== -->
        <Grid Grid.Column="2" RowDefinitions="Auto,*,Auto">

            <Border Grid.Row="0" Background="{StaticResource PanelLightGreenBrush}" Classes="panel-header">
                <TextBlock Foreground="{StaticResource TextGreenBrush}" Classes="h1">
                    <Run x:Name="RunAnnotListPrefix" />
                    <Run Text="{Binding CurrentAnnotations.Count}" />
                    <Run Text=")" />
                </TextBlock>
            </Border>

            <ListBox Grid.Row="1" ItemsSource="{Binding CurrentAnnotations}"
                     SelectedItem="{Binding SelectedAnnotation}">
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="vm:AnnotationDisplayItem">
                        <Border Padding="6,4" BorderBrush="{StaticResource BorderExtraLightGrayBrush}" BorderThickness="0,0,0,1">
                            <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto,Auto">
                                <Border Grid.Column="0" Grid.RowSpan="2"
                                        Width="12" Height="12" CornerRadius="2"
                                        Background="{Binding ClassColor}" Margin="0,0,8,0"
                                        VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Grid.Row="0"
                                           Text="{Binding ClassName}" FontWeight="SemiBold"
                                           FontSize="12" />
                                <TextBlock Grid.Column="1" Grid.Row="1"
                                           Text="{Binding BBoxText}"
                                           Classes="secondary" />
                                <Button Grid.Column="2" Grid.RowSpan="2"
                                        Content="X" FontSize="10"
                                        Background="Transparent" Foreground="{StaticResource DeleteButtonRedBrush}"
                                        Command="{Binding $parent[ListBox].((vm:AnnotationViewModel)DataContext).DeleteAnnotationCommand}"
                                        CommandParameter="{Binding}"
                                        VerticalAlignment="Center" Padding="4,2" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

             <StackPanel Grid.Row="2" Spacing="4" Margin="8">
              <TextBlock Text="{Binding StatusMessage}" FontSize="11"
                           Foreground="{StaticResource ButtonBlueBrush}" TextWrapping="Wrap"
                           IsVisible="{Binding StatusMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                           Margin="0,0,0,4" />
                 <Button x:Name="BtnMarkComplete" HorizontalAlignment="Stretch"
                        Command="{Binding MarkCompletedCommand}" />
                <Button x:Name="BtnSaveAnnotation" HorizontalAlignment="Stretch"
                        Command="{Binding SaveAnnotationCommand}" />
                <Separator Margin="0,4" />
                <Button x:Name="BtnExportYolo" HorizontalAlignment="Stretch"
                        Command="{Binding ExportDatasetCommand}" />
                <Button x:Name="BtnImportYolo" HorizontalAlignment="Stretch"
                        Command="{Binding ImportYoloCommand}" />
                <Separator Margin="0,4" />
                <Button x:Name="BtnImportLabelMe" HorizontalAlignment="Stretch"
                        Click="OnImportLabelMeClick"
                        Background="{StaticResource ButtonBlueBrush}" Foreground="{StaticResource TextLightBrush}" />
                                 <Button x:Name="BtnImportCustomJson" HorizontalAlignment="Stretch"
                        Click="OnImportCustomJsonClick"
                        Background="{StaticResource ButtonPurpleBrush}" Foreground="{StaticResource TextLightBrush}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>